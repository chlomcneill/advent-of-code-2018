import re
import numpy as np
import matplotlib.pyplot as plt

f = open("/Users/mcneillc/Documents/advent-of-code-2018/Day 3/Day3input.txt","r")
claims = f.readlines()
f.close()

# size = 1500
# grid = []

# for y in range(size):
#    grid = grid + [[]]
#    for x in range(size):
#       grid[y] = grid[y] + [0]

# print(grid)

# xvalues = np.array(range(10))
# yvalues = np.array(range(10))

# xx, yy = np.meshgrid(xvalues, yvalues)

# plt.plot(xx, yy, marker = '.', color = 'k', linestyle = 'none')
# plt.show()

